name: Build Android APK

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: 🟢 Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: mobile_app/package-lock.json

      - name: Install Dependencies
        working-directory: ./mobile_app
        run: npm install

      - name: Grant Execute Permission to Gradlew
        working-directory: ./mobile_app/android
        run: chmod +x gradlew

      - name: Build Android APK
        working-directory: ./mobile_app/android
        run: ./gradlew assembleRelease -x lint -x check --no-daemon -Dorg.gradle.jvmargs="-Xmx2048m -XX:MaxMetaspaceSize=512m" --stacktrace

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-release
          path: mobile_app/android/app/build/outputs/apk/release/app-release.apk
